import { TobiShopValidators } from './tobi-shop-validators';

describe('TobiShopValidators', () => {
  it('should create an instance', () => {
    expect(new TobiShopValidators()).toBeTruthy();
  });
});
